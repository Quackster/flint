// list_life: the full List lifecycle. Every operation, both flavours
// (int and string elements), built up with add() (the stdlib has no
// literals — a collection is grown one element at a time).
// Build: flintc src/stdlib/coll.flint src/stdlib/mem.flint examples/list_life.flint -o list_life
import std.List;
int main() {
    // construction: add() one at a time
    List l = new List();
    l.add(10); l.add(20); l.add(30);
    List e = new List();
    printi(e.size());             // 0
    print("\n");
    // add / get / set / size
    l.add(40);
    printi(l.size());            // 4
    print("\n");
    printi(l.get(1));           // 20
    print("\n");
    l.set(0, 99);
    printi(l.get(0));           // 99
    print("\n");
    // contains: int compare; string content compare
    printi(l.contains(20));     // 1
    print("\n");
    printi(l.contains(77));     // 0
    print("\n");
    // insert at an index (tail shifts right; i >= size appends)
    l.insert(1, 5);
    printi(l.size());          // 5
    print("\n");
    printi(l.get(1));         // 5
    print("\n");
    printi(l.get(2));         // 20 (was shifted)
    print("\n");
    // remove(i): tail shifts left; no-op when i >= size
    l.remove(0);
    printi(l.get(0));         // 5
    print("\n");
    printi(l.size());         // 4
    print("\n");
    // sort (signed int ascending) and reverse
    l.sort();
    printi(l.get(0));         // 5
    print("\n");
    printi(l.get(3));         // 40
    print("\n");
    l.reverse();
    printi(l.get(0));         // 40
    print("\n");
    // join: ints via itoa, strings verbatim
    string j = l.join("-");
    print(j);               // 40-30-20-5
    print("\n");
    // clear
    l.clear();
    printi(l.size());         // 0
    print("\n");
    // string elements (kind = 1): sort by content, join verbatim
    List s = new List();
    s.kind = 1;
    s.add("b");
    s.add("a");
    s.add("c");
    printi(s.contains("a"));   // 1 (content compare)
    print("\n");
    printi(s.contains("z"));   // 0
    print("\n");
    s.sort();
    print(s.join("|"));       // a|b|c
    print("\n");
    // int list
    List t = new List();
    t.add(7); t.add(8); t.add(9);
    printi(t.size());         // 3
    print("\n");
    printi(t.get(2));       // 9
    print("\n");
    return 0;
}
