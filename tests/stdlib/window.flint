// std.Window test: the pure helpers (fixed-point conversion, key-code
// map, display-name parsing, string comparison). Fully deterministic —
// never opens a display, so it is stable headless and on a desktop.
import std.Window;

int main() {
    // ---- fixed-point (24.8) ----
    printi(Window.fixed(256)); print(" ");
    printi(Window.fixed(1023)); print(" ");
    printi(Window.fixed(-256)); print(" ");
    printi(Window.fixed(0)); print("\n");

    // ---- key-code map (Linux evdev keycodes, US layout) ----
    printi(Window.keyChar(16) == 113 ? 1 : 0); print(" ");   // 'q'
    printi(Window.keyChar(1) == 27 ? 1 : 0); print(" ");    // Escape
    printi(Window.keyChar(11) == 48 ? 1 : 0); print(" ");   // '0'
    printi(Window.keyChar(28) == 13 ? 1 : 0); print(" ");   // Return
    printi(Window.keyChar(57) == 32 ? 1 : 0); print(" ");   // Space
    printi(Window.keyChar(14) == 8 ? 1 : 0); print(" ");    // Backspace
    printi(Window.keyChar(99) == 0 ? 1 : 0); print("\n");   // unmapped

    // ---- display-name parsing ----
    printi(Window.parseDisplay("wayland-2")); print(" ");
    printi(Window.parseDisplay("wayland-0")); print(" ");
    printi(Window.parseDisplay("wayland-12")); print(" ");
    printi(Window.parseDisplay("no digits") == 0 ? 1 : 0); print("\n");

    // ---- string helpers ----
    printi(Window.strEq("wayland-0", "wayland-0") ? 1 : 0); print(" ");
    printi(Window.strEq("a", "b") ? 1 : 0); print(" ");
    printi(str.cmp(Window.xdgPath("/run/user/1000", "wayland-3"),
                   "/run/user/1000/wayland-3") == 0 ? 1 : 0); print("\n");
    return 0;
}
