// Simple test: just create a thread and join it
package test;

void thread_fn(int arg) {
    sys.write(1, "hello", 5);
}

int main() {
    int tid = sys.threadCreate(@thread_fn, 0);
    sys.threadJoin(tid);
    sys.write(1, "done", 4);
    return 0;
}
