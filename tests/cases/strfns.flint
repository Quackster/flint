int main() {
    // substring
    *byte s = str.substring("hello world", 0, 5);
    println(str.cmp(s, "hello") == 0 ? 1 : 0);
    *byte s2 = str.substring("hello world", 6, 11);
    println(str.cmp(s2, "world") == 0 ? 1 : 0);
    // indexOf
    println(str.indexOf("hello world", "world") == 6 ? 1 : 0);
    println(str.indexOf("hello world", "xyz") == -1 ? 1 : 0);
    // replace
    *byte r = str.replace("aaa", "a", "b");
    println(str.cmp(r, "bbb") == 0 ? 1 : 0);
    *byte r2 = str.replace("hello", "l", "L");
    println(str.cmp(r2, "heLLo") == 0 ? 1 : 0);
    return 0;
}
