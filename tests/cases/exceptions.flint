class MyError {
    int code;
    void MyError(int c) { this.code = c; }
}

int caught_value() {
    try {
        throw 42;
    } catch (int e) {
        return e;
    }
    return -1;
}

int caught_class() {
    try {
        throw 99;
    } catch (int e) {
        return e;
    }
    return -1;
}

int no_throw() {
    try {
        return 7;
    } catch (int e) {
        return -1;
    }
}

int main() {
    // Basic throw/catch
    int v1 = caught_value();
    println(v1 == 42 ? 1 : 0);

    // Another throw/catch
    int v2 = caught_class();
    println(v2 == 99 ? 1 : 0);

    // No throw - try block returns normally
    int v3 = no_throw();
    println(v3 == 7 ? 1 : 0);

    // Throw in main
    try {
        throw 123;
    } catch (int e) {
        println(e == 123 ? 1 : 0);
    }

    return 0;
}
