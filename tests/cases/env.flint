int main() {
    // PATH is set in the test environment
    *byte p = env.get("PATH");
    println(p != null ? 1 : 0);
    println(strlen(p) > 0 ? 1 : 0);
    // missing variable returns null
    *byte m = env.get("BOX_TEST_NONEXISTENT_XYZ");
    println(m == null ? 1 : 0);
    return 0;
}
